apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "repaircafe.fullname" . }}-test-connection"
  labels:
    {{- include "repaircafe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "repaircafe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
