apiVersion: v1
kind: Service
metadata:
  name: {{ include "repaircafe.fullname" . }}
  labels:
    {{- include "repaircafe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repaircafe.selectorLabels" . | nindent 4 }}
